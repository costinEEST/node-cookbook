module.exports = {
  add: (firstNumber, lastNumber) => firstNumber + lastNumber,
  subtract: (firstNumber, lastNumber) => firstNumber - lastNumber,
  multiply: (firstNumber, lastNumber) => firstNumber * lastNumber,
  divide: (firstNumber, lastNumber) => firstNumber / lastNumber,
};
